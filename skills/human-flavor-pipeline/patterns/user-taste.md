# 个人禁忌层(0.x 兼容入口)

1.0 起本文件不再存放任何人的个人规则,也不参与默认加载。这样可以避免公开安装者误继承维护者偏好,并防止用户把客户真稿写进会随版本更新的仓库文件。

- 通用默认:`profiles/default.md`
- 仓库内可选示例:`profiles/lens.md`,仅在明确指定时启用
- 真实个人 / 团队规则:放在安装目录外的本地 Profile

兼容旧调用时,把对本文件的读取请求重定向到当前明确启用的 Profile;没有明确 Profile 就使用 `profiles/default.md`。事实规则始终高于 Profile。
